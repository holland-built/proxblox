#!/usr/bin/env bash
[ -n "${STUB_LOG:-}" ] && echo "RSYNC" >> "$STUB_LOG"
case "$*" in *"--rsync-path=sudo -n rsync"*) [ -n "${STUB_LOG:-}" ] && echo "RSYNC-SUDO" >> "$STUB_LOG" ;; esac
[ "${STUB_HALT:-0}" = 1 ] && { echo "STUB-HALT: rsync" >&2; exit 99; }
exit 0
